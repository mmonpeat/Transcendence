PRIMERO:
Descargar el contenido de este repo: 
https://github.com/Contubernio/ft_transcendence

SEGUNDO:
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d

TERCERO:
Para las pruebas de las bbdd acceder a 
http://localhost:8081/menu y dar al botón test de base de datos

CUARTO:
Para pruebas del pong en linea abrir en dos navegadores o 2 sesiones del mismo navegador si no no funciona multijugador:
http://localhost:8081/menu
y pulsar el botón test de Websocket el proceso esperará a que haya dos jugadores, si se abren más seran espectadores

LIBERAR ESPACIO:
para eliminar todas las pruebas realizadas y liberar espacio
docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)
docker image prune -a
nos preguntara si o si le decimos si

docker rm -f $(docker ps -aq) && docker rmi -f $(docker images -q)

docker exec –it name_continer bash

docker run -it --rm --entrypoint bash mytra-api

make up

make logs

# Acceder a un contenedor
make shell-<continername>